<UserControl x:Class="WpfApp.Premium"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="25,0,25,0" >
            <Label Content="Премиум" FontFamily="Montserrat" FontSize="26" FontWeight="Bold"/>
            <Label Content="Если нужно больше" FontFamily="Montserrat" FontSize="16" FontWeight="Medium"/>
        </StackPanel>
        <DockPanel Grid.Row="1">
            <Label Content="На данный момент недоступно" FontFamily="Montserrat" FontSize="26" FontWeight="Bold" Margin="0,150,0,0" HorizontalAlignment="Center"/>
        </DockPanel>
    </Grid>
</UserControl>
